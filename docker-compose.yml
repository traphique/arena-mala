services:
  arena-mala:
    build: .
    ports:
      - "4000:4000"
    env_file:
      - .env
    environment:
      PORT: "4000"
      SAMPLE_STORAGE_DIR: /app/samples
    volumes:
      - arena_samples:/app/samples

volumes:
  arena_samples:
